spring:
  application:
    name: transfer-service

  spring:
    security:
      oauth2:
        resourceserver:
          jwt:
            issuer-uri: http://localhost:8080/realms/bank-realm

        client:
          registration:
            transfer-service:
              provider: keycloak
              client-id: transfer-service
              client-secret: jP64RAuVqIwD029W5rI4cyqGzFO84sgn
              authorization-grant-type: client_credentials
          provider:
            keycloak:
              issuer-uri: http://localhost:8080/realms/bank-realm

server:
  port: 8083