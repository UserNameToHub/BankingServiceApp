apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-database-secret
type: Opaque
data:
  username: {{ .Values.databases.username | b64enc }}
  password: {{ .Values.databases.password | b64enc }}